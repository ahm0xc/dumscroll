{
  "name": "dumscroll",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "format": "pnpm biome format --write && biome check --write"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.3",
    "prettier": "^3.2.5",
    "turbo": "^2.0.6",
    "typescript": "^5.4.5"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  }
}
